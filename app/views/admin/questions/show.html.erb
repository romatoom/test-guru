<h1><%= t(".title", question_title: @question.body) %></h1>

<div class="mb">
  <p>
    <%= t(".description", test_title:@question.test.title, test_level: @question.test.level).html_safe %>
  </p>
</div>

<h2><%= t(".answer_options") %></h2>

<% if @question.answers.any? %>
  <table class="mb">
    <thead>
      <tr>
        <th><%= t("common.questions.answer_text") %></th>
        <th><%= t("common.questions.true_answer") %>?</th>
        <th colspan="3" class="text-align-center"><%= t("common.actions") %></th>
      </tr>
    </thead>

    <tbody>
      <%= render @question.answers %>
    </tbody>
  </table>
<% else %>
  <p class="mb"><% t(".no_answers") %></p>
<% end %>

<br>

<%= link_to t("common.back"), admin_test_path(@question.test), class: "button" %>
<%= link_to t(".add_answer"), new_admin_question_answer_path(@question), class: "button success" %>
