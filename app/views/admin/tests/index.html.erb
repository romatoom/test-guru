<h1><%= t("common.tests.all_tests") %></h1>

<% if @tests.any? %>
  <table class="mb">
    <thead>
      <tr>
        <th><%= t("common.id") %></th>
        <th><%= t("common.tests.title") %></th>
        <th class="text-align-center"><%= t("common.tests.level") %></th>
        <th><%= t("common.tests.category") %></th>
        <th><%= t("common.tests.author") %></th>
        <th class="text-align-center"><%= t("common.tests.questions_count") %></th>
        <th colspan="4" class="text-align-center"><%= t("common.actions") %></th>
      </tr>
    </thead>

    <tbody>
      <%= render @tests %>
    </tbody>
  </table>
<% else %>
  <p class="mb"><%= t("common.tests.no_tests") %></p>
<% end %>

<br>

<%= link_to t(".create_test"), new_admin_test_path, class: "button success" %>
